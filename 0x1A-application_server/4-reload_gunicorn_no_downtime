#!/usr/bin/env bash
# Gracefully reloads Gunicorn.
#shellcheck disable=SC2009


# Send HUP signal to Gunicorn processes
pkill -HUP -f gunicorn

# Wait for old workers to finish processing requests
while pgrep -f gunicorn >/dev/null; do
    sleep 1
done
