#!/usr/bin/env bash
# Gracefully reloads Gunicorn.
#shellcheck disable=SC2009

# Get the process ID of the Gunicorn master process
master_pid=$(pgrep -o -f "gunicorn: master")

if [ -z "$master_pid" ]; then
    exit 1
fi

# Send HUP signal to the Gunicorn master process
kill -HUP "$master_pid"
